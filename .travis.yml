language: php

php:
   - 5.3

before_script:
   - wget http://getcomposer.org/composer.phar
   - ant -Dsystem.composer=composer.phar -Dcomposer.dev=--dev init

script:
   - ant -f build-dev.xml moodle-automation-init CI-build

notifications:
   email:
      - covex.mobile@gmail.com
